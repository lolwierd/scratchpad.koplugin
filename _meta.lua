local _ = require("gettext")
return {
    name = "scratchpad",
    fullname = _("Scratchpad"),
    description = _([[A quick scratchpad for jotting and re-reading notes while you read. Per-book by default, plus a shared global pad. Open it from the reader menu or bind it to a gesture.]]),
}
